#!/bin/bash
# Guardian eval environment — GCS/Vertex in prod, BigQuery in homolog
#
# Videos (GCS): brandlovers-prod
# Vertex AI / Gemini: brandlovers-prod  
# BigQuery data: brandlovrs-homolog
#
# Usage: source ~/.openclaw/workspace/.env.guardian-eval before running evals
export GOOGLE_CLOUD_PROJECT=brandlovers-prod
export GOOGLE_CLOUD_LOCATION=us-central1
export BIGQUERY_PROJECT=brandlovrs-homolog

# Verify configuration
echo "✓ Guardian eval env loaded:"
echo "  GOOGLE_CLOUD_PROJECT=$GOOGLE_CLOUD_PROJECT"
echo "  GOOGLE_CLOUD_LOCATION=$GOOGLE_CLOUD_LOCATION"
echo "  BIGQUERY_PROJECT=$BIGQUERY_PROJECT"
