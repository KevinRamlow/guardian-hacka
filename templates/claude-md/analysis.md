# Analysis Task Instructions

## Output Format

- Structured markdown with data tables, counts, and percentages
- Save results to `/tmp/` — do NOT commit analysis outputs to the repo

## Include

- SQL queries used (so results are reproducible)
- Sample sizes and date ranges
- Compare against production baseline (**86.78%**) when applicable

## Delivery

Log a summary with key numbers in your `done` message. The orchestrator reads logs, not files.
