apiVersion: v1
kind: Service
metadata:
  name: anton-openclaw
  labels:
    app: anton-openclaw
spec:
  type: ClusterIP
  selector:
    app: anton-openclaw
  ports:
    - port: 18789
      targetPort: 18789
      protocol: TCP
