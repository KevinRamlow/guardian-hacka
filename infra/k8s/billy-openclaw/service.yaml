apiVersion: v1
kind: Service
metadata:
  name: billy-openclaw
  labels:
    app: billy-openclaw
spec:
  type: ClusterIP
  selector:
    app: billy-openclaw
  ports:
    - port: 18789
      targetPort: 18789
      protocol: TCP
