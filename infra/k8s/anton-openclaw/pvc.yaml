apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: anton-openclaw-state
  labels:
    app: anton-openclaw
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard-rwo
  resources:
    requests:
      storage: 10Gi
