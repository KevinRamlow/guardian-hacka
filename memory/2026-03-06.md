# Memory Log — 2026-03-06

## 00:59-01:22 UTC — Session Start (Post-Compaction from 03-05)

### DM Audit (01:00 UTC)
- Luca got 6 messages in 3 hours (too many), Rapha 1, Matheus 1, Victor 1
- Caio said don't clean up

### Billy VM Auth Fixed (01:23 UTC)
- Copied Anton's gcloud creds via rsync
- Cloud SQL Proxy working, MySQL confirmed (61,854 rows)
- All 3 Billy features unblocked: CAI-74, CAI-75, CAI-79

### CAI-35: Eval Crash-Resilience — COMPLETED (01:07-01:15 UTC)
- Branch: `fix/eval-crash-resilience` pushed
- Incremental progress.jsonl saves, --resume flag, error classification, graceful shutdown

### Self-Improvement Architecture (01:03 UTC)
- Designed 6-layer architecture, Caio said "don't build yet, plan is fine"

### Self-Improvement Phases 1-4 Spawned (01:24 UTC)
- CAI-98 (Phase 1: Observation) — completed 01:27 UTC
- CAI-99 (Phase 2: Analysis) — spawned
- CAI-100 (Phase 3: Experimentation) — spawned
- CAI-101 (Phase 4: Autonomous Loop) — spawned

## 02:16-02:38 UTC — Dashboard + Pipeline Setup + Hypothesis Agents

### CAI-102: Slack-Linear Sync — Done
### CAI-103: Reprocess Skill Fix (API-first) — Done (5 min)
### CAI-104: CreatorAds Design Analysis — Done
### CAI-105: Guardian Multi-Hypothesis Analysis — Done

### Dashboard Chaos (02:18-02:23 UTC)
- Old Python server kept respawning on port 8765
- Found it was child of openclaw-gateway
- Killed old server, started Express + WebSocket dashboard
- Dashboard crashed on null subagent data, fixed error handling
- Final: working on 0.0.0.0:8765 with SSH tunnel

### GCP Auth Fixed (02:20 UTC)
- Caio's Claude Code sent application_default_credentials.json to clawdbot
- BQ + Vertex AI confirmed working
- Guardian eval blocker cleared

### CAI-75: Brand Creation — Done (via API endpoint in user-management-api)

### Caio's Instructions (02:23-02:38 UTC)
1. **6-hour status reports** — cron created
2. **5 hypothesis agents** spawned for Guardian +5pp:
   - CAI-106: Prompt Engineering (severity 3 boundary)
   - CAI-107: Archetype v2 (granular taxonomy)
   - CAI-108: LLM-as-Judge (auto-correction)
   - CAI-109: Memory Pipeline tuning
   - CAI-110: TIME_CONSTRAINTS routing fix
3. **PRs: Draft only, reviewer fonsecabc only, FULL metrics required**
4. **Billy auto-spawn**: every task done → next task launches automatically
5. **8 AM wake-up report** scheduled
6. **Subagent limit bumped to 10**
7. Billy tasks (CAI-76, 82, 88, 93, 95) were orphaned after steer displaced them → moved back to Todo

### CRITICAL FAILURE: Gateway Restart Killed All Agents (02:38 UTC)
- Editing openclaw.json + gateway restart killed all 5 hypothesis agents at ~2 min runtime
- None completed, none logged completion
- Gateway entered 10h crash loop (02:38 → 12:48 UTC)
- 8 AM wake-up cron failed with thinking block error
- **Root cause:** SIGUSR1 kills all running agents during drain

## 09:59-12:53 UTC — Recovery

### Caio Checks In (09:59, 12:19 UTC)
- Gateway finally came back at 12:48 UTC
- Delivered overnight report: all 5 hypothesis agents lost, 0 progress

### Pipeline Architecture Discussion (13:04 UTC)
Caio: "Create a literal queue pipeline"
- **Option A: Cron + File** (2h) — JSON state, 60s cron, survives restarts
- **Option B: Manager Agent** (4-6h) — persistent session
- **Option C: Shell Daemon** (6-8h) — systemd service
- **Decision: Option A** — Caio said "do it yourself, no agent"

### Two Pipelines Created (13:12-13:21 UTC)
- **LONG** — 5 workers, Claude Code (ACP), 25 min stall, 3 retries
- **FAST** — 10 workers, subagents, 10 min stall, 2 retries
- pipeline-ctl.sh for management
- System crontab every 60s for health + OpenClaw cron every 2 min for spawning

### ACP Issues (13:23-13:25 UTC)
- `defaultAgent: "code"` not in acpx registry (has: codex, claude, gemini, opencode, pi)
- Changed to `"claude"` → but npm install hangs (network)
- Switched long pipeline to subagent runtime as fallback
- Multiple restarts killed agents repeatedly (Caio frustrated: "Come on")

### Anthropic Model Testing Task Added (13:34 UTC)
- Caio: "Add task to test Guardian text-to-text agents with Anthropic models"
- Account for prompt engineering for Anthropic models
- Evaluate at least 3 times for consistency
- Long task for the pipeline

### Hypothesis Agents Finally Spawned (14:16 UTC)
- After multiple restart failures, all 5 spawned and survived
- At 14:33: Caio killed 3 (archetype, judge, routing), kept severity3 + memory
- Branches pushed:
  - CAI-106: `experiment/severity-3-boundary-tuning`
  - CAI-107: `feat/archetype-standardization-v2` (PR #30)
  - CAI-108: `feat/llm-judge-borderline-cai-108` (PR #31)
  - CAI-110: `fix/time-constraints-routing`
  - CAI-109: no branch yet

### Pipeline Confirmed Working (14:36 UTC)
- Test task: queue → spawn → run → done in 7s
- Full flow validated end-to-end
- All branches pushed to origin

### Queue Not Auto-Running (14:48 UTC)
- Caio noticed queue wasn't auto-spawning from backlog
- I incorrectly said "no auto-queue" — actually it was built but context was lost
- Killed 2 overtime agents (34 min): hyp-a-severity3, hyp-d-memory

### Ralph Research (14:50 UTC)
- Caio shared https://github.com/snarktank/ralph
- Researched: bash loop spawning fresh AI instances per PRD story
- Key insight: fresh context each iteration, progress.txt for learnings, explicit acceptance criteria
- Plan: build ralph-loop skill adapting pattern for OpenClaw sessions_spawn

### Slack History Read (14:50 UTC)
- Caio: "read our chat and update ur memory because we cannot have that gap"
- Read full DM history from D0AK1B981QR channel
- Updated this memory file with complete timeline

## 16:18-16:48 UTC — Dashboard Agent Visibility Fix

### Problem: Dashboard Not Showing ACP Agents (16:47 UTC)
- Dashboard only read main agent session store via `openclaw sessions` CLI
- ACP agents stored separately in `/root/.openclaw/agents/claude/sessions/`
- Result: 5 hypothesis agents invisible to dashboard

### Solution: Multi-Agent Store Reader (16:47 UTC)
- Rewrote `collectData()` in dashboard server.js
- Now reads ALL agent directories: billy, claude, claude-code, code, codex, main
- Filters work sessions: CAI-*, pipeline-*, gua-*, guardian-*, experiment-*, or subagent/acp keys
- Skips: main session, cron jobs, slack channels, billy main

### Filter Debugging (16:48 UTC)
- First attempt: 0 agents shown (too strict filtering)
- Adjusted: keep all claude/claude-code/code agents, plus labeled work from main
- Final verification: 14 work sessions visible (9 claude, 5 main)
  - CAI-108, CAI-109, CAI-110 (active hypothesis agents)
  - pipeline-cai-106/107/108/109/110 (completed spawns)
  - Billy tasks (CAI-78/80/81/83/84)
- Dashboard now has full visibility into multi-agent orchestration

## Key Lessons (Today)

1. **Gateway restart = agent death** — NEVER restart while agents running. Batch config changes.
2. **ACP not reliable** — acpx network issues, wrong agent names. Use subagents as fallback.
3. **Pipeline architecture works** — cron + file state survives restarts
4. **Context loss after compaction** — must read Slack history to recover gaps
5. **Caio's PR rules**: Draft only, reviewer fonsecabc only, full eval metrics required
6. **Ralph pattern**: Fresh context per iteration > long-running sessions
7. **Dashboard visibility**: Must read all agent stores, not just main (claude agents especially)

## Current State (16:48 UTC)
- **Active agents:** 0 (all recent work completed)
- **Recent agents (last 24h):** 14 visible in dashboard
  - 3 active hypothesis agents: CAI-108, CAI-109, CAI-110 (21-25 min ago)
  - 6 pipeline spawns (completed)
  - 5 Billy tasks (completed 3-4h ago)
- **Pipeline:** Built (long + fast), working, not auto-feeding from backlog
- **Dashboard:** Fixed to show multi-agent orchestration (reads all agent stores)
- **Hypothesis branches:** All pushed to origin
- **Pending:** Ralph-loop skill implementation, Anthropic model testing task

## 22:29 UTC — Resiliência 24/7 (Prioridade Máxima por Caio)

### Fixes Implementados:
1. **auto-queue.sh** — checa Linear Todo, conta agents ativos, enfileira spawns
2. **completion-checker.sh** — detecta agents terminados/timeout, limpa queue
3. **dashboard-watchdog.sh** — reinicia dashboard se morrer
4. **HEARTBEAT.md reescrito** — queue processing obrigatório em cada heartbeat
5. **Cron "Queue + Health Monitor"** — roda a cada 5 min, executa os 3 scripts + spawna tasks
6. **Billy systemd** — tentativa (VM unreachable, retry quando voltar)
7. **Crons velhos removidos** — Pipeline Spawner (broken), Auto-Spawn Billy (obsoleto)

### O Que Falta:
- Billy VM systemd (VM down, precisa retry)
- Gateway crash resilience (precisa systemd unit pro Anton gateway também)
- Testar o ciclo completo: Linear Todo → auto-queue → spawn → complete → notify

## 22:37 UTC — Pre-Compaction Memory Flush

### Billy VM Powered Off
- Caio informed Billy VM (89.167.64.183) is powered off intentionally
- Will be replaced with a bigger VM later
- All Billy work paused until new VM is ready

### Resiliência 24/7 System (IMPLEMENTED)
**Scripts created:**
- `scripts/auto-queue.sh` — checks Linear Todo, counts active agents, queues spawns to `/root/.openclaw/tasks/spawn-queue/`
- `scripts/completion-checker.sh` — detects finished/timed-out agents, cleans queue
- `scripts/dashboard-watchdog.sh` — starts dashboard when Caio active, kills after 30min idle
- Dashboard port (8765) closed by default, opened only when active

**Cron jobs:**
- "Queue + Health Monitor" — every 5 min, runs auto-queue + completion-checker + dashboard-watchdog + spawns pending tasks
- Removed old broken crons: Pipeline Spawner, Auto-Spawn Billy

**HEARTBEAT.md rewritten:**
- Queue processing MANDATORY every heartbeat
- Never return HEARTBEAT_OK without checking queue first
- Priority: spawn queue → agent health → dashboard → timed checks

**Linear tasks created for remaining fixes:**
- CAI-112: Billy VM systemd (blocked — VM off)
- CAI-113: Anton gateway systemd + state backup (agent spawned)
- CAI-114: Auto-retry failed agents + notifications (agent spawned)
- CAI-115: Guaranteed completion notifications (covered by CAI-114)

### Security Hardening (COMPLETED)
- Both servers: UFW active (SSH only), SSH password auth OFF, fail2ban active
- .env files chmod 600, API keys removed from dashboard code
- Dashboard port closed by default
- No exposed ports except SSH

### Billy Sync (COMPLETED)
- SOUL.md: correct Billy identity (was Anton's copy)
- TOOLS.md: all tools documented
- ackReaction: eyes enabled
- Streaming: block mode
- Presentation protocol: matplotlib for data (NOT nano-banana)
- matplotlib + plotly installed on Billy VM

### Key Fixes Today
- Dashboard: reads all agent session stores (/root/.openclaw/agents/*/sessions.json), not just CLI
- Dashboard token auth: `anton-cockpit-956eeb5220bb355b`
- Linear API: working (key in .env.linear, tested)
- Slack logging: working (slack-linear-post.sh → C0AJQ99GW6P channel)

### Messages Cutting Off
- Caio reported Slack messages getting cut off (too long)
- Must keep responses SHORT for Slack

### Critical Learnings
- Gateway restart kills ALL agents (SIGUSR1 drain) — need pre-restart backup
- `openclaw sessions` CLI shows all as `kind: "direct"` — useless for subagent detection
- ACP agents are in `/root/.openclaw/agents/claude/sessions/` (separate store)
- Dashboard node process dies when run via nohup but survives when run via `node -e "require('./server.js')"`
- UFW doesn't fully work on Hetzner VPS (sysctl errors) but rules are set

## 22:43 UTC — Linear Organization + Full Backlog

### Linear Structure Created
**Labels (8):** 🛡️ resilience, 🔄 queue, 📊 monitoring, 🚧 guardrails, 🧪 testing, ⚡ feature, 🤖 billy, 🔍 guardian
**Projects (4):** Anton Resiliência 24/7, Guardian +5pp, Billy Platform, Infrastructure
**39 total tasks** all tagged with labels + projects

### Resiliência Backlog (24 new tasks: CAI-112 to CAI-139)
**Priority 1 (spawned):**
- CAI-116+117: Systemd + pre-restart backup
- CAI-129+130+121: Spend limit + dedup + task-to-prompt
- CAI-133: E2E pipeline test
- CAI-113: Gateway resilience (duplicate of 116, agent running)
- CAI-114: Auto-retry (agent running)

**Priority 2-3 (queued in Linear Todo):**
- CAI-118: Agent timeout watchdog
- CAI-119: Heartbeat failure alerting
- CAI-120: Gateway crash loop detection
- CAI-122-124: Queue improvements
- CAI-125-128: Monitoring tasks
- CAI-131-132: Credential rotation + disk space
- CAI-134-135: More tests
- CAI-136-139: Features (output collection, work hours, git commit, debrief)

### Billy VM Status
- Powered off by Caio — getting bigger VM
- CAI-112 (Billy systemd) → Blocked
- All Billy skills tasks paused
- Will resume when new VM ready

### Dashboard
- Token: anton-cockpit-956eeb5220bb355b
- URL: http://89.167.23.2:8765
- Port opened/closed on demand
- Still crashes periodically — watchdog restarts it
- CAI-127 tracks proper fix

### Active Crons (9 total)
- Queue + Health Monitor (5min) — auto-queue + completion-checker + dashboard watchdog
- Memory Update (30min)
- Linear Task Sync (30min)
- 6h Capabilities Report
- Self-improvement: daily observe, experiment eval, weekly analyze, Monday full loop, monthly meta

## 22:48 UTC — Triple Logging + Disk Persistence

### Logging System (CRITICAL — implemented per Caio's priority)
**Every agent event now saved 3 ways:**
1. **Disk** → `/root/.openclaw/tasks/agent-logs/{taskId}.log` (per task) + `master.log` (global)
2. **Linear** → API comment on task
3. **Slack** → Posted to sync channel

**Scripts:**
- `scripts/agent-logger.sh` — disk logger (taskId, event, message)
- `scripts/capture-agent-transcript.sh` — saves full agent transcript on completion
- Hooked into: `linear-log.sh` (every log call), `completion-checker.sh` (on complete/timeout)

**Disk log format:**
```
[2026-03-06 22:48:11 UTC] [event] CAI-XX: message | context
```

**Master log auto-rotates at 10K lines**

### 8 Agents Running at 22:48 UTC
- auto-queue-pipeline, CAI-68, CAI-88 (older spawns)
- CAI-113, CAI-114, CAI-116+117, CAI-129+130+121, CAI-133 (resiliency batch)

## 23:01 UTC — Critical Realizations & Corrections

### MAJOR BUG: Zombie Claude Processes
- **20 orphan Claude Code processes** consuming 6.3GB RAM doing nothing
- Gateway restarts leave ACP processes alive (no parent to clean up)
- Session store shows 0 tokens for ALL agents = none ever produced real output
- `subagents list` returns empty = OpenClaw doesn't track ACP agents after restart
- **Fixed:** Killed all zombies, freed 5GB RAM (28GB free now)

### CRITICAL MISTAKE: Not Using Ralph Loop
- Caio explicitly said to use ralph-loop for spawning agents (discussed 14:50 UTC)
- Ralph loop skill EXISTS at /root/.openclaw/workspace/skills/ralph-loop/
- Instead, I kept doing raw `sessions_spawn` without structure
- Raw spawns = no progress tracking, no acceptance criteria, no file-based memory
- Ralph = stories with criteria, progress.txt for learnings, fresh context per iteration
- **Must use ralph-loop.sh for ALL future agent spawns**

### Langfuse Configuration
- Created hook: /root/.openclaw/hooks/langfuse-tracer.js
- Hooks into onAgentTurnEnd and onSessionSpawn
- Posts to Langfuse ingestion API (traces + generations)
- Anton's project keys (NOT Guardian's):
  - Public: [REDACTED]
  - Secret: [REDACTED]
- Env vars in: /root/.openclaw/.env, ~/.bashrc, dashboard/.env
- **Note:** Hook may not work if OpenClaw hooks don't support onAgentTurnEnd/onSessionSpawn events (needs testing)

### Dashboard Data Mismatch
- Dashboard reads session store → shows sessions as "alive" based on updatedAt
- But ACP agents don't write tokens to session store → always shows 0
- Need to fix: count actual `claude` processes OR read ACP internal state
- Caio frustrated by inaccurate dashboard data

### Triple Logging System (Implemented)
- agent-logger.sh → disk log per task + master.log
- capture-agent-transcript.sh → saves full transcript on completion
- linear-log.sh now hooks into agent-logger.sh
- completion-checker.sh calls both on complete/timeout

### Zero Gateway Restarts Policy
- Every restart kills agents and creates zombies
- NO MORE gateway restarts until pre-restart-backup.sh is working
- Any config changes must wait or be done manually in openclaw.json

### Caio's Message: "eu preciso que vc tenha um processo claro para clean up start e restart de agentes do prd ralph loop"
- Need clear process for: cleanup, start, restart of ralph loop agents
- This is THE priority now
