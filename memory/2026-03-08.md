# 2026-03-08

## Major Infrastructure Refactoring: Native OpenClaw Migration

Caio researched autonomous LLM agents as co-workers (30+ sources) and refactored my architecture. I now run on native OpenClaw primitives instead of custom bash orchestration. Key changes:

### What Changed

**1. Native Heartbeat Enabled (openclaw.json)**
- Configured `agents.defaults.heartbeat` with 5-minute interval
- Active hours: 08:00-23:00 São Paulo timezone
- Target: Slack (HEARTBEAT_OK silently dropped, alerts forwarded)
- This is the big change — I now proactively check in every 5 minutes through the gateway

**2. Sub-Agent Configuration (openclaw.json)**
- Added `maxSpawnDepth: 2` (sub-agents can spawn children)
- Added `maxChildrenPerAgent: 5` (max active children per session)
- Existing `maxConcurrent: 10` preserved

**3. Compaction Configuration**
- `reserveTokensFloor: 20000` already configured — auto-distills sessions to daily memory
- Note: `softThresholdTokens` was NOT a valid key in v2026.3.2 — removed after `openclaw doctor` flagged it

**4. HEARTBEAT.md Updated**
- Now includes auto-queue logic (Priority 2): fetch Linear Todo tasks, check slots, spawn agents
- Now includes eval-completion logic (Priority 1): check for completed evals, trigger next iteration
- Added structured escalation chain: self-heal → retry → escalate → block
- Replaces functionality from disabled launchd jobs

**5. Lobster Workflow Created**
- `workflows/guardian-eval-pipeline.yaml` — deterministic eval loop
- Steps: preflight → implement → evaluate → analyze → decide
- Loop max 3 iterations with approval gate for production ship

**6. Launchd Jobs Consolidated (6 → 4)**
- DISABLED: `com.anton.auto-queue.plist` (absorbed into native heartbeat)
- DISABLED: `com.anton.eval-completion-check.plist` (absorbed into native heartbeat)
- KEPT: watchdog (PID management), linear-sync, langfuse-scraper, gcp-token-push

**7. SOUL.md Updated**
- Key tools section now references native OpenClaw capabilities
- Monitoring section updated to reflect hybrid (heartbeat + launchd) architecture
- Lobster workflows referenced for Guardian eval iteration

**8. MEMORY.md Updated**
- Scheduling section rewritten for hybrid architecture
- Native features documented (sub-agents, compaction, memory search, Lobster)

### What Still Uses Custom Scripts (and why)
- `spawn-agent.sh` — Registry tracking + Linear logging not in native sub-agents
- `agent-watchdog-v2.sh` — Process-level PID monitoring requires bash, not LLM prompts
- `agent-registry.sh` — Custom registry complementing native sub-agent management
- `agent-stream-monitor.py` — Real-time progress reporting to Slack + Linear

### Research Report
Full 30-source research report saved to: `~/Documents/Autonomous_LLM_Agents_Anton_Refactor_Research_20260308/`
